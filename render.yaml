services:
  - type: worker
    name: telegram-pdf-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false   # Render will prompt for this on first deploy
